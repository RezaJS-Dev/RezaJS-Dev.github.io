/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  output: "export",
  distDir: "./dist",
  output: "export",
  // Set the build output directory to 'dist'
  distDir: "dist",
};

export default nextConfig;
